window sizing for canvas abstract to a pct of the size of the window object
remove canvas and go to css

given a length and digit boundries
1. generate a  an array of 20 in length of 1-4 digits  G R Y B

reset game
score = 0;
new sequence generated

comp moves
given a number itterate through an array for that given length
1. get me number
2. get me an array for that given length
3. play the sequence for each item in the array


check players moves
1. get piece they clicked
2. update player moves
3.check if piece clicked matches the index of computer moves for the given number of moves
